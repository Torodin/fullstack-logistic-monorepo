# Shared environment variables (root)
NODE_ENV=development

# Local PostgreSQL container credentials
POSTGRES_USER=logistic_user
POSTGRES_PASSWORD=logistic_pass
POSTGRES_DB=fullstack_logistic

# Prisma and API database connection
DATABASE_URL=postgresql://logistic_user:logistic_pass@localhost:5432/fullstack_logistic?schema=public
